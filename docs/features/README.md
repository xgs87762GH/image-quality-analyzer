# 功能说明

本文档详细说明项目的所有功能特性。

## 📋 目录

- [核心功能](#核心功能)
- [质量评估](#质量评估)
- [AI分析](#ai分析)
- [元数据管理](#元数据管理)
- [Web界面](#web界面)
- [回收站功能](#回收站功能)

## 核心功能

### 1. 图像质量分析

系统可以对图像进行全面的质量评估，包括：

- **模糊度检测**: 使用Laplacian方差算法检测图像清晰度
- **亮度分析**: 计算平均亮度，检测过曝或欠曝
- **信息熵**: 评估图像的信息量和复杂度
- **BRISQUE评分**: 无参考图像质量评估（可选）
- **审美评分**: 基于CLIP模型或AI模型的审美评估（可选）

### 2. 数据库存储

- **SQLite数据库**: 所有分析结果持久化存储
- **自动去重**: 基于文件哈希自动识别重复图像
- **快速查询**: 支持按评级、标签、质量分数查询
- **统计分析**: 自动生成质量统计报告

### 3. 元数据管理

#### XMP元数据写入

- **写入元数据**: 将分析结果写入图像文件的XMP元数据
- **兼容性**: 兼容主流图像管理软件（Lightroom, Bridge等）
- **元数据字段**:
  - Rating: 1-5星评级
  - Label: 质量标签（High/Medium/Low/Very Low）
  - Subject: 关键词列表
  - Description: 详细指标JSON

#### 完整元数据读取

- **读取所有元数据**: 支持读取图像的完整元数据（EXIF、GPS、XMP、IPTC等）
- **分类展示**: 元数据按类别组织（基本信息、相机信息、GPS信息、XMP信息等）
- **优雅降级**: 如果ExifTool不可用，自动使用数据库中的元数据
- **Web界面支持**: 在图像详情页面可以查看完整元数据

## 质量评估

### 评估指标

1. **模糊度 (Blur Score)**
   - 算法: Laplacian方差
   - 范围: 0-∞（越高越清晰）
   - 阈值: < 100 视为模糊

2. **亮度 (Brightness)**
   - 算法: 平均像素强度
   - 范围: 0-255
   - 理想值: 150左右

3. **信息熵 (Entropy)**
   - 算法: 信息熵计算
   - 范围: 0-8
   - 阈值: < 5 视为信息量不足

4. **BRISQUE (可选)**
   - 算法: 无参考图像质量评估
   - 范围: 0-100（越低越好）
   - 需要: OpenCV quality模块

5. **审美评分 (可选)**
   - 方式1: CLIP模型（本地）
   - 方式2: AI模型（GPT-4V, Claude, Gemini, Ollama）
   - 范围: 0-10分

### 综合评分

系统将各项指标归一化到0-100范围后，按权重计算综合质量分数：

```
质量分数 = 模糊度归一化值×权重 + 亮度归一化值×权重 + 熵归一化值×权重 + 审美归一化值×权重
```

**归一化方法**：
- **模糊度**: 基于阈值归一化（阈值通常为100）
- **亮度**: 基于理想值150，考虑容差范围
- **信息熵**: 基于阈值归一化（阈值通常为8）
- **审美评分**: 从0-10映射到0-100

**权重配置**（可在 `utils/constants.py` 中调整）：
- 模糊度: 40% (0.4)
- 亮度: 30% (0.3)
- 信息熵: 30% (0.3)
- 审美评分: 30% (0.3，如果启用)

**阈值配置**：
- 高质量: ≥80分
- 中等质量: 60-79分
- 低质量: 40-59分
- 极低质量: <40分

### 评级和标签

- **1-5星评级**: 根据质量分数自动映射
- **质量标签**: High / Medium / Low / Very Low
- **问题检测**: 自动识别模糊、亮度问题、低熵、高失真等问题

## AI分析

### 支持的AI模型

1. **GPT-4 Vision** (OpenAI)
   - 需要: OpenAI API密钥
   - 特点: 强大的图像理解能力

2. **Claude 3** (Anthropic)
   - 需要: Anthropic API密钥
   - 特点: 优秀的图像分析能力

3. **Gemini Pro Vision** (Google)
   - 需要: Google API密钥
   - 特点: 多模态理解能力

4. **Ollama** (本地)
   - 需要: 本地Ollama服务
   - 推荐模型: llava, qwen2-vl, qwen3-vl
   - 特点: 完全本地化，无需API密钥

### AI分析功能

- **图像描述**: 自动生成图像内容描述
- **质量评估**: AI评估图像质量
- **自定义评估**: 支持自定义评估问题和答案格式
- **审美评分**: 使用AI模型进行审美评估

### 自定义评估问题

支持设置自定义评估问题，例如：
- "是否为手机截图"
- "是否包含人物"
- "拍摄场景类型"

答案格式支持（return_type）：
- **array**: 从预定义选项中选择（如：["是", "否"]、["高", "中", "低"]）
- **float**: 0-1之间的浮点数评分
- **text**: 自由文本描述

详细格式说明请查看 [评估格式说明](../guides/evaluation-format.md)

## 元数据管理

### XMP元数据写入

系统支持将分析结果自动写入图像的XMP元数据，兼容主流图像管理软件。

#### 支持的XMP标签

系统使用标准XMP标签，确保最佳兼容性：

**XMP Core标签**（最常用，兼容性最好）：
- `xmp:Rating` - 星级评分 (1-5)，支持Lightroom、Capture One、Luminar等
- `xmp:Label` - 颜色标签，用于质量分类
- `xmp:MetadataDate` - 元数据修改日期

**Dublin Core标签**（标准元数据）：
- `dc:Subject` - 关键词/主题（使用追加模式，保留原有关键词）
- `dc:Description` - 描述信息（追加质量分析摘要，不覆盖原有描述）
- ⚠️ **不写入** `dc:Title` - 保护原有标题
- ⚠️ **不写入** `dc:Creator` - 保护原有作者信息

**IPTC Core标签**（新闻摄影常用）：
- `Iptc4xmpCore:Keywords` - 关键词数组格式（某些软件需要）

#### 关键词生成

系统通过多种方式生成关键词：

1. **质量分析关键词**：基于图像质量指标自动生成
   - 质量标签（HighQuality, MediumQuality等）
   - 问题检测（blurry, brightness_issue, low_entropy等）

2. **AI分析关键词**：从AI分析文本中智能提取
   - 自动识别图像内容相关的关键词
   - 支持中文和英文关键词提取
   - 从评估结果中提取结构化关键词

3. **关键词合并**：自动合并并去重，确保XMP中的关键词丰富且准确

#### 软件兼容性

**完全兼容** ⭐⭐⭐⭐⭐：
- **Adobe Lightroom** - 完整支持所有XMP标签，可读取评分、标签、关键词、描述
- **Adobe Bridge** - 作为Adobe生态的一部分，完整支持

**良好兼容** ⭐⭐⭐⭐：
- **Capture One** - 支持核心标签（评分、标签、关键词、描述）
- **ON1 Photo RAW** - 支持关键词树、描述、评分、颜色标签

**基础兼容** ⭐⭐⭐：
- **Luminar** - 支持基础标签（标题、描述、标签、评分、关键词）
- **XnView** - 支持大部分XMP标准标签
- **digiKam** - 支持XMP标准标签
- **Photo Mechanic** - 支持XMP元数据读写

#### 元数据保护 ⚠️ 重要

**系统严格保护原有元数据，绝对不会覆盖或影响：**

- ✅ **个人信息**：作者、版权、联系方式等
- ✅ **时间信息**：拍摄时间、创建时间、修改时间等（仅更新MetadataDate）
- ✅ **地点信息**：GPS坐标、地理位置、地址等
- ✅ **摄影参数**：EXIF数据（ISO、光圈、快门速度、焦距、设备型号、镜头信息等）
- ✅ **编辑历史**：原始编辑记录、工作流程数据等

**系统仅写入以下质量分析相关字段：**
- `xmp:Rating` - 质量评分（使用追加模式，不覆盖）
- `xmp:Label` - 质量标签（使用追加模式，不覆盖）
- `dc:Subject` - 分析关键词（使用追加模式，保留原有关键词）
- `Iptc4xmpCore:Keywords` - 关键词数组（使用追加模式）
- `dc:Description` - 质量分析描述（追加到现有描述后，使用`[质量分析]`标记）
- `xmp:MetadataDate` - 元数据修改日期（仅更新此字段）

**技术保障：**
- 使用ExifTool的`-preserve`选项保护所有现有元数据
- 使用`+=`追加模式而非`=`覆盖模式
- 仅操作预定义的安全字段白名单

#### ExifTool安装（自动下载）

**系统会在首次启动时自动下载ExifTool，无需手动操作！**

- 启动项目时，系统会自动检测ExifTool
- 如果未找到，会自动从 https://exiftool.org/ 下载对应平台版本
- 下载后自动解压到 `exiftool/` 目录
- 完全自动化，用户无需任何操作

**系统检测优先级：**
1. 项目内的 `exiftool/` 目录（自动下载）
2. 系统PATH中的ExifTool（如果已安装）

**手动安装（可选）**：
如果自动下载失败，可以手动安装：
- **Windows**: 下载并添加到系统PATH
- **macOS**: `brew install exiftool`
- **Linux**: `sudo apt-get install libimage-exiftool-perl`

#### 使用建议

1. **跨平台兼容**：使用标准XMP标签（评分、关键词、描述）确保最佳兼容性
2. **关键词管理**：启用AI分析可自动生成丰富的关键词，提升图像可搜索性
3. **数据备份**：系统默认创建备份（.bak文件），可在设置中关闭
4. **ExifTool安装**：推荐下载到项目目录，无需系统安装，便于部署
5. **安全可靠**：系统设计确保不会丢失或覆盖任何原有元数据

#### XMP元数据写入

系统可以将分析结果写入图像文件的XMP元数据，使用标准XMP标签确保最佳兼容性。

#### 写入的XMP字段

**核心字段**（所有软件都支持）：
- **Rating** (`xmp:Rating`): 1-5星评级，基于质量分数自动计算
- **Label** (`xmp:Label`): 质量标签（HighQuality/MediumQuality/LowQuality/VeryLowQuality）
- **Subject** (`dc:Subject`): 关键词列表，包含质量标签和问题检测关键词
- **Description** (`dc:Description`): 质量分析指标摘要和AI分析描述

**扩展字段**（可选）：
- **Keywords** (`Iptc4xmpCore:Keywords`): 关键词数组格式（某些软件需要）
- **MetadataDate** (`xmp:MetadataDate`): 元数据修改日期

#### AI关键词提取

当启用AI分析时，系统会：
1. 从AI分析文本中自动提取关键词（支持中英文）
2. 从评估结果中提取结构化关键词
3. 与质量分析生成的关键词合并去重
4. 写入XMP的Subject和Keywords字段

这样可以大大丰富图像的元数据，提升在图像管理软件中的可搜索性。

#### 软件兼容性

**完全兼容** ⭐⭐⭐⭐⭐：
- **Adobe Lightroom** - 完整支持所有XMP标签，可读取评分、标签、关键词、描述
- **Adobe Bridge** - 作为Adobe生态的一部分，完整支持

**良好兼容** ⭐⭐⭐⭐：
- **Capture One** - 支持核心标签（评分、标签、关键词、描述）
- **ON1 Photo RAW** - 支持关键词树、描述、评分、颜色标签

**基础兼容** ⭐⭐⭐：
- **Luminar** - 支持基础标签（标题、描述、标签、评分、关键词）
- **XnView** - 支持大部分XMP标准标签
- **digiKam** - 支持XMP标准标签
- **Photo Mechanic** - 支持XMP元数据读写

### 元数据读取

支持从XMP元数据读取：
- 已分析的图像可以直接读取结果
- 兼容其他软件写入的元数据
- 支持按XMP标签筛选图像（评级、标签、关键词等）

## Web界面

### 主要功能

1. **图像列表**
   - 查看所有已分析的图像
   - 搜索和筛选功能
   - 分页浏览
   - 图像预览（直接使用原图，支持懒加载）

2. **图像详情**
   - 查看完整分析结果
   - 质量指标可视化
   - AI分析结果展示
   - 自定义评估结果

3. **统计分析**
   - 整体质量统计
   - 标签分布
   - 评级分布
   - 质量分数分布

4. **重复检测**
   - 基于文件哈希查找重复图像
   - 显示重复图像组

5. **回收站**
   - 查看已删除的图像
   - 恢复或永久删除

### 设置功能

- **基础设置**: 自动分析、审美评估方式、每页显示数量
- **分析设置**: 并发分析数量
- **AI模型设置**: 选择AI模型、配置API密钥
- **自定义评估**: 设置评估问题和答案格式
- **图片源目录**: 管理图片源目录列表

## 回收站功能

### 功能特性

1. **软删除**
   - 删除的图像移动到回收站（不真正删除文件）
   - 保留原目录结构
   - 记录删除时间

2. **恢复功能**
   - 从回收站恢复图像
   - 自动移回原位置
   - 处理路径冲突

3. **永久删除**
   - 真正删除图像文件
   - 删除数据库记录

### 使用场景

- 误删恢复
- 批量清理
- 临时删除（后续决定是否永久删除）

## 批量处理

### 支持的操作

- **批量分析**: 同时分析多张图片
- **批量导入**: 从多个目录导入图片
- **批量删除**: 批量删除图片
- **批量恢复**: 批量恢复图片

### 性能优化

- **并发处理**: 可配置并发数量（默认3张）
- **进度显示**: 实时显示分析进度
- **错误处理**: 单个失败不影响整体

## 自动导入

### 功能说明

- **自动检测**: 启动时自动从配置的目录导入图片
- **目录验证**: 自动验证目录是否存在
- **脏数据清理**: 自动清理源文件不存在的记录
- **静默模式**: 支持静默导入（不输出详细信息）

## 扩展功能

### 图像显示

- **直接使用原图**: 系统直接使用原图显示，无需生成缩略图
- **懒加载**: 支持图片懒加载，提高页面加载速度
- **格式支持**: 支持所有常见图像格式（JPEG、PNG、WebP等）

### 系统信息

- **环境信息**: Python版本、系统信息
- **依赖检查**: 检查必需依赖是否安装
- **模型状态**: 检查AI模型是否可用
